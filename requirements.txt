# FFXI Gear Set Optimizer - Requirements
# Install with: pip install -r requirements.txt

# Web Framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
tzdata
python-multipart

# System tray support
pystray>=0.19.0
Pillow>=10.0.0

# Build tools (optional, for creating executable)
pyinstaller>=6.0.0

# Optional: Numba for accelerated optimization (recommended)
numba>=0.58.0
